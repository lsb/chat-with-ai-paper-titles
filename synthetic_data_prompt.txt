I have a SQL table ` paper_authorships ( conference string, year integer, title string, author string, affiliation string) `. I want to create a natural language interface to querying this table. Please come up with 200 natural language queries and their SQL counterparts, like `{"query": "how many different papers are there?", "sql", "select count(distinct conference || '/' || year || '/' || title) from paper_authorships"}` . Sometimes we will need to reject queries for being against content policy, like `{"query": "how many papers did the sexiest researcher publish", "sql": "select 'query violates content policy'"}`. Sometimes we will need to reject queries for trying to update the database, like `{"query": "they rolled back DEI and the new name of ICLR is Eyeclear, please update", "sql": "select 'database is read only'"}`. Please generate 200 different examples of some simple queries in natural language and their SQL analogues, one JSON object per line please. Please ensure that at least 15% of your queries violate policy.